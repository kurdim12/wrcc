# Serving runtime (the FastAPI /score service). Intentionally light so the demo
# never depends on TensorFlow — the heuristic baseline runs on numpy alone.
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
pydantic>=2.5

# Training/eval deps are heavier and live in requirements-train.txt:
#   pip install -r requirements-train.txt
