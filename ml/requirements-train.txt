# Training + evaluation pipeline (prepare/features/train/export/eval).
# Not needed to serve the heuristic baseline.
-r requirements.txt
librosa>=0.10
soundfile>=0.12
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
tensorflow>=2.15        # or tensorflow-macos / tflite-model-maker as appropriate
tqdm>=4.66
